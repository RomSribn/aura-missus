# AURAT-0077-011 — Смержено в develop

Дата: 2026-09-18

| | |
|---|---|
| Ветка | `feature/AURAT-0077-bff-play-refunds` |
| Коммит | `407d294` — 34 файла, +1868 / −78 |
| `develop` | `d3c8ec8` → **`c546279`**, запушено в origin |
| missus | «Already up to date» — намеренно, один коммит в конце задачи |
| Слейв | остаётся на ветке для послемёржевых документов |

Обновлены простаивающие слейвы 3, 4, 5.

## Порядок выкатки — важен

В проде и на dev с `BILLING_ENABLED=true` сервис **не поднимется** без
`PLAY_RTDN_AUDIENCE` и `PLAY_RTDN_SERVICE_ACCOUNT_EMAIL`. Это намеренно
(зеркало проверки сервисного аккаунта Play: маршрут списывает деньги, и пустая
проверка хуже отсутствующей), но означает жёсткий порядок:

1. завести подписки → узнать audience;
2. положить обе переменные в окружение;
3. только потом деплой.

Наоборот — сервис не стартует.
