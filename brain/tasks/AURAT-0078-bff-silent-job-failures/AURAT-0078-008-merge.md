# AURAT-0078-008 — Смержено в develop

Дата: 2026-09-18

| | |
|---|---|
| Ветка | `feature/AURAT-0078-bff-silent-job-failures` |
| Коммит | `cd05885` |
| `develop` в маноре | `1843a68` → **`676d0b5`** |
| Пуш на origin | `develop -> develop` — успешно |
| missus | коммитов пока нет (по правилу — один коммит в конце задачи); пуш дал предупреждение, это ожидаемо |
| Праздные слэйвы | `slave-2`, `slave-4`, `slave-5` обновлены на новый master |

Слэйв остался на ветке — под шаги после мёржа.

## Проверки перед мёржем

```
1237 тестов, 98 наборов — зелено (было 1207 / 96)
eslint, tsc --noEmit, nest build, prettier — чисто
```

Отдельно проверено, что после негативной проверки сторожа ничего не осталось:
`TarotProcessor` на базовом классе, все одиннадцать процессоров наследуют
`LoggingWorkerHost`, `job.data` не логируется нигде в `src/jobs/`.

## Дальше — живая проверка в маноре

Из слэйва её сделать нельзя: стек поднимается только в маноре.

1. Поднять стек на новом `develop`.
2. Временно сломать адрес Chatwoot в конфигурации dev — сверка ходит раз в
   60 секунд, ждать недолго.
3. Ждать строку: уровень `error`, `context: ReconciliationProcessor`,
   `queue: reconciliation`, `attempt: 1`, `attempts: 1`, текст ошибки.
4. Убедиться, что полезной нагрузки в строке нет.
5. Вернуть конфигурацию, дождаться прохода без ошибки.

Дополнительно, если захочется увидеть разделение уровней: уронить `fcm-fanout`
(`attempts: 3`) — две строки `warn`, затем `error`.
