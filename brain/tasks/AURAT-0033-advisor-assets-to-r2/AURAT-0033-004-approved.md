# AURAT-0033-004 — Принято

Дата: 2026-08-20

Владелец разрешил закрывать и идти дальше по списку; проверку аватаров на
устройстве делает отдельно.

## Состояние

Переезд **уже применён в проде** — он не ждал этого мержа: восемь файлов лежат в
`aura-assets`, `assets.aura-app.cc` их отдаёт, `ADVISOR_ASSETS_BASE_URL`
переключён в Coolify, контейнер пересоздан. Репозиторий этим коммитом лишь
догоняет реальность и убирает мёртвый хост из шаблонов и фикстур.

Проверено мной: восемь ключей **из базы** отдают 200 с верным `content-type` и
`cache-control`, размеры сверены; базовый адрес подтверждён внутри контейнера.
Не проверено мной: как каталог выглядит в приложении — это на владельце.

## Остаётся владельцу

- Открыть приложение и убедиться, что аватары на месте.
- Решить про удаление файлов на AWS: пока они лежат, откат — одна переменная.
- Отозвать токен `aura-assets-storage`, он был нужен на одну операцию.
- Локальный `.env` BFF (не в репозитории) всё ещё указывает на брошенный
  AWS-бакет — стоит поменять, иначе локальная разработка останется на хранилище,
  которое может пропасть.
