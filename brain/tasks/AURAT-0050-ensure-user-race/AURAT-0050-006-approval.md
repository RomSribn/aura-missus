# AURAT-0050-006 — Аппрув

Дата: 2026-09-01
Решение владельца: **approved** — «Да, делай».

Утверждён вариант «оба конца»:

- непустой `update` возвращает нативный `INSERT … ON CONFLICT DO UPDATE`
  (лечение — атомарность на стороне Postgres);
- `catch P2002` → перечитать строку (страховка — план выбирает Prisma, и в
  нашем коде этот выбор не виден).

Плюс два теста (параллельный вызов и тест на сам план), правка комментария над
`ensureUser`, строка в `TECH-DEBT.md`.

Отдельно принято к сведению: находка в `aura-app`
(`push-registration.ts:31`, `catch {}` без ретрая) здесь не чинится — другой
манор, ID минтит `aura-app-manor`.

## Следующий шаг

007 — исполнение.
