# AURAT-0065-006 — Одобрение

Дата: 2026-09-08

Владелец принял спеку `005` и все три решения в предложенном виде:

- **D1** — `<Image source={{ uri, headers }}>`, без файлового кэша. Повтор с
  обновлённым токеном лечится в фиче, при неудаче рисуется орб.
- **D2** — лист по нажатию «Change photo»: снять / выбрать / убрать. Добавление
  к нарисованному, принято сознательно: иначе убрать фотографию нечем.
- **D3** — фотография показывается и в шапке настроек (`ProfileCard`), кнопки
  там нет.

Размер при выборе — 1024 px, `quality: 0.9` (умолчание спеки, возражений нет).

Дальше: этап исполнения.
