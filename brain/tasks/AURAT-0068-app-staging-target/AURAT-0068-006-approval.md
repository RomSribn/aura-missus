# AURAT-0068-006 — Одобрение

Дата: 2026-09-14

Владелец принял спеку `005` целиком, D1 — в рекомендованном виде:

- **D1** — `aab:staging` / `apk:staging` с billing-флагами инлайном; голые
  `aab` / `apk` удаляются; `aab:prod` / `apk:prod` заводятся при запуске, вместе
  с настоящим продом.

Остальное (новый `env/.env.staging`, тесты, README, пример в отказе резолвера,
комментарий в `env.ts`, `AURAS-0002` при закрытии) — как в спеке.

Дальше: этап исполнения.
