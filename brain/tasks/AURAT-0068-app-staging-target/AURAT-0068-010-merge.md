# AURAT-0068-010 — Повторный мёрж: versionCode 18

Дата: 2026-09-14

Владелец одобрил повторный мёрж.

- `wts-finish slave-0`: `aura-app` `develop` = **`faf0bb8`** (мёрж `a21147c`),
  push в origin прошёл (`933ca1d..faf0bb8`).
- В маноре `android/app/build.gradle:100` — `versionCode 18`.
- Слот остаётся на ветке. Missus не закоммичен.

Дальше — в маноре: `npm run aab:staging` → internal testing → установка из Play
→ проверка по `005`.
