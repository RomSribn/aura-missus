# AURAT-0068-018 — Принято

Дата: 2026-09-14
Статус: **done**

Владелец закрыл задачу после проверки сборки `versionCode 19` из Play.

## Что сделано

- **Target `staging`** — `env/.env.staging` на `https://bff-dev.aura-app.cc`;
  `aab:staging` / `apk:staging` вместо голых `aab` / `apk`; `aab:prod` — при
  запуске. `env/.env.prod` не тронут.
- **Аватар на Android** — `source={[source]}`: RN 0.85 теряет `headers` у
  объектного источника, фотография уходила без токена (401). Подтверждено на
  устройстве.
- **versionCode 18 и 19** — сборки internal testing на bff-dev.

`aura-app` `develop` = **`506a318`**, в origin. Коммиты задачи: `6b6d9d3`,
`a21147c`, `4de2342`, `405fcbb`.

## Итог проверки

| Что | Результат |
|---|---|
| Сборка ходит на bff-dev | да |
| Фотография профиля | видна (сборка 19) |
| Вложения в чате | грузятся (сборка 19) |
| Арт карт таро | **нет** — ключи `.png` в BFF против залитых `.jpg`; вынесено в `AURAT-0069` (`aura-bff`) |

## Оставлено

- `AURAT-0069` — ключи колоды в `aura-bff`.
- Старый алиас `bff.aura-app.cc` снимать только после того, как тестировщики
  обновятся до сборки на `staging`.
- Шапка `env/.env.prod` всё ещё говорит «Deliberately empty» — не трогалась по
  указанию владельца.
- `AURAS-0002` шаг 3 — команды сборки переписаны этим же missus-коммитом.
