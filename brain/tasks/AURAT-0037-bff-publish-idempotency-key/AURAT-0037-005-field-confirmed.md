# AURAT-0037-005 — Подтверждено владельцем в поле

Дата: 2026-08-21
Записано из: `aura-bff-manor` (манор, только документация)
Закрывает: раздел «Чего нет» в `AURAT-0037-002-both-halves-merged.md`

## Подтверждение

Владелец, на развёрнутом BFF (`develop` @ `fc62f29`) и с приложением, в котором
`mergeServerMessages` уже замержен: **«всё работает»**.

Это тот самый прогон, которого не хватало. Прежние две проверки его не заменяли:

| когда | что показывало | почему не приёмка |
|---|---|---|
| до мёржа половины BFF | висело «Not delivered» | ключ ещё не возвращался |
| после, вручную | «жму ещё раз, прогружается, в одном экземпляре» | доказывало ключ и отсутствие дубля, но **нажимали** |
| сейчас | работает | обе половины развёрнуты впервые |

Сквозная приёмка `AURAT-0037` выполнена: «Not delivered» гаснет само, дубля нет.

## Состояние слотов

`slave-1` освобождён, все пять слотов свободны. Обе половины `AURAF-0011`
(приём и отправка) закрыты и проверены; в фиче остаётся открытой одна строка —
**008 по колонке `UI`**, запись голосового в приложении, заблокированная
конфликтом Nitro-модуля с платёжным рельсом. Со стороны BFF там `✓`: сервер
принимает голосовое как любой другой аудиофайл.

## Что этот день стоит помнить

Три раза подряд правильный разбор указывал на неверное место, и каждый раз это
заканчивало **измерение**, а не рассуждение:

- `AURAI-0003` — причина усечения была во фронте, хотя разбор уверенно указывал
  на объявленную длину; замер 9 из 9 вложений в шести режимах это закрыл, а
  заодно нашёл настоящий, но другой дефект.
- Тег `v0.11.0` считался непоставленным — он был на origin с самого начала.
- Деплой считался ручным с незамерженной ветки — Coolify перевели на `develop`
  тем же утром.

Общее у всех трёх: утверждение было верным **когда-то** и осталось в документе.
Дешёвая проверка стоит дешевле правильного рассуждения о старых данных.
