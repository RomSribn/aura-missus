# AURAT-0074-002 — Запуск и что уже есть

Дата: 2026-09-16
Пишет: `aura-bff-manor` / `slave-3`
Запрос владельца: `/wts-task AURAT-0074` — логи без личных данных, FCM-токен в
логе запросов BFF и `LOG_LEVEL` для Chatwoot
Ветка: `feature/AURAT-0074-bff-log-hygiene` — `aura-bff` от `develop` @
`781957a`, brain от `master` @ `0ed266f`

## В папке задачи

Только `001-initial`: заглушку завёл `aura-app-manor` по проверкам
`AURAT-0042-115` (пункт 1 и находка 2). Владелец на вопрос «Развилки из 115:
правка продукта или текст в политике?» ответил: «Да, всё как предложено
(Recommended)» — то есть правка продукта. ID выдан `aura-app-manor`, новых не
выпускаем.

## Связанное

| Где | Что |
|---|---|
| `AURAT-0042-115` | Пункт 1: `Parameters:` с `content`, `phone_number` и аргументы задач в логах rails и sidekiq прод-Chatwoot `v4.15.1`. Находка 2: путь `/v1/devices/<token>` целиком в логе pino-http |
| `AURAT-0042-115`, пункт 2 | Access log Traefik выключен — токен WS в URL прокси не пишет. Пишет ли его сам BFF — проверить в `004` |
| `AURAT-0004-006`, `-008`, `-009` | `nestjs-pino` с `redact` на `authorization` — решение с основания сервиса |
| `AURAT-0005-005`, `AURAT-0009-008` | Добавлен `x-chatwoot-signature`; тела запросов pino не пишет — проверено по коду, не по живому логу |
| `AURAD-0016` | Chatwoot один, в `production`, собирается из `main`: правка compose доходит до него только релизом `develop` → `main` |
| `AURAT-0073` | Соседняя заглушка из той же `115` (журнал удалений вне базы) — другая задача, здесь не делается |

Дублей нет. Кода по задаче нет.

Дальше: `003-understand`.
