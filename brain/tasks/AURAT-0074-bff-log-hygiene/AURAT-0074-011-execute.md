# AURAT-0074-011 — Исполнение `010`: регистр в пути устройства

Дата: 2026-09-16
Не закоммичено, застейджено.

| Файл | Что |
|---|---|
| `src/common/logging/http-logger.options.ts` | `DEVICE_TOKEN_IN_PATH = /(\/devices\/)[^/?#]+/gi`, замена `'$1[redacted]'`: совпадение без учёта регистра, сегмент в логе в том регистре, в каком пришёл (`/v1/DEVICES/[redacted]`). Комментарий дополнен |
| `src/common/logging/http-logger.options.spec.ts` | Тест 404 стал табличным: `/v2/devices/<token>?source=app` и `/v1/DEVICES/<token>` |

**Контрольный прогон:** со старой регуляркой (без `i`) новый случай падает,
остальные 4 проходят; с исправленной — 5 из 5.

`devices%2F<token>` не трогается — по решению владельца (`010`).

## Проверки

`lint` чисто · `tsc --noEmit` чисто · `jest` — 82 набора, **1 024** теста, все
зелёные · `build` — exit 0 · prettier на изменённых файлах — чисто.

Дальше: ревью владельца → коммит в ветку → мёрж в `develop` (одобрение).
