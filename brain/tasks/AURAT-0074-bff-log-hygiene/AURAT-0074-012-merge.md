# AURAT-0074-012 — Мёрж в `develop`

Дата: 2026-09-16
Владелец: «коммить и мёржи в develop», затем «Да, мёрж» на вопрос merge gate.

- Коммит в ветке: `fa84be7` `feat(logs): AURAT-0074 — логи без личных данных,
  срок хранения в очередях` (13 файлов, +499 / −58).
- `wts-finish slave-3`: `aura-bff` `develop` @ **`abe2039`**, push
  `d54b07d..abe2039` — ок.
- Между стартом ветки (`781957a`) и мёржем в `develop` влился `AURAT-0073`
  (`d54b07d`, журнал удалений). Конфликтов нет; `src/jobs/` он не трогает.
  Слитое дерево проверено отдельно (копия `abe2039` вне репо, `prisma generate`):
  `tsc` чисто, `jest` — **83 набора, 1 060 тестов, все зелёные**.
- Missus на этом шаге не коммитится (так по процессу). `wts-finish` предупредил
  «missus push failed»: у манора `aura-missus` `master` отстаёт от `origin` на 3
  коммита из `aura-app-manor` (`d51b7aa`, `b615c50`, `b95994a` — бриф
  `AURAT-0042` по решениям этой задачи). Учесть при `wts-release`.

## Проверка в маноре (`development`)

1. Деплой `bff-dev` поднялся; в логе старта нет ошибок графа.
2. Регистрация устройства из dev-сборки — в логе `aura-bff (development)`
   путь `/v1/devices/[redacted]`, токена нет.
3. Открыть файл-вложение — в строке запроса нет имени файла и `res.headers`,
   только `res.statusCode`.
4. Через час после деплоя — в логе джобы `queue-retention` (или её выполненная
   запись в Redis); строка `queue records past retention removed`, если было что
   чистить — в `development` есть записи старше суток от прежних тестов.

Не в `development`: `LOG_LEVEL` Chatwoot — только релиз `main`; Scheduled Task —
панель Coolify.

Дальше: проверка владельцем.
