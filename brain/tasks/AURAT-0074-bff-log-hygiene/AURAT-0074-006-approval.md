# AURAT-0074-006 — Спека `005` утверждена

Дата: 2026-09-16
Владелец: «Утверждаю»

- **Q1 — A:** остаток (аргументы упавших задач Sidekiq на WARN) принять и
  описать честно: `AURAS-0004` + передача в `aura-app-manor` для брифа политики
  `AURAT-0042`. Монтирования initializer в Chatwoot нет.
- **Q2 — A:** `LOG_LEVEL: warn` жёстко в compose, без ручки в панели.
- Спека `005` утверждена целиком.

## Пришло после вопроса, до исполнения

Из `aura-app-manor` (сессия `manor-b8`) — расширение объёма со словами
владельца «добавь срок в AURAT-0074». Это ответ на вопрос той сессии: срок жизни
записей в очередях BFF (`age` рядом с `count`) или честная строка в политике.
Основание — правка брифа `AURAT-0042` (brain `d51b7aa`), уточнение к п. 6
`AURAT-0042-115`: очереди чистятся только по счёту, `dump.rdb` на диске, в
данных задач `chatwoot-webhook` — текст ответа оператора, в `fcm-fanout` —
FCM-токены; dead set Sidekiq Chatwoot хранит аргументы 180 дней.

`005` не переписывается: добавка идёт отдельной спекой `007` со своими
решениями владельца, исполнение — обеих вместе после её утверждения.

Дальше: `007-spec-queue-retention`.
