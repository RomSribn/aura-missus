# AURAT-0071-006 — Одобрение спеки

Дата: 2026-09-14

**Одобрено владельцем целиком** («Да, вся спека»):

- параметризация `deploy/coolify/chatwoot.compose.yml` через `CW_RAILS_ALIAS` и
  `CW_FRONTEND_URL` с прод-значениями по умолчанию;
- комментарии compose под два экземпляра;
- правка `provision-prod.rb`: `AURA_WEBHOOK_URL` обязателен, шапка под
  реальность, команда провижининга dev-экземпляра;
- `AURAS-0004` — в конце задачи поверх свежего `missus`, если `aura-app-manor`
  не возьмёт это на себя.

Дальше: исполнение (`007`).
