# AURAT-0072-008 — Смержено в `develop`

Дата: 2026-09-16
Владелец: «давай тогда мерж, я чекну на девайсе» и «Да, мёрж» на гейте

## Что ушло

| Репозиторий | Что | Результат |
|---|---|---|
| `aura-bff` | `feature/AURAT-0072-bff-chatwoot-contact-name` (`c52f6a8`) → `develop` | merge `781957a`, запушен `6698f1e..781957a` |
| `aura-missus` | — | шаги `002`–`008` застейжены в слоте, коммит — при закрытии задачи |

Код владелец в IDE не смотрел: решил проверить поведение на устройстве. Ворота
в слоте перед коммитом были чистыми (`007`).

## Что включилось

Деплой `development` (`bff-dev.aura-app.cc`). Миграций нет. После старта:

1. Новый контакт получает имя при создании, смена имени в профиле уходит на
   контакт джобой `contact-name`.
2. Старые тестовые контакты получат имена после
   `node dist/contact-names.js` в контейнере BFF — один прогон.

Дальше: проверка владельцем на устройстве.
