# AURAT-0031-010 — Смёржено в develop

Дата: 2026-08-21

## Что уехало

| | |
|---|---|
| Ветка | `feature/AURAT-0031-chat-attachments-receive` |
| Коммит фичи | `c9f129c` |
| `develop` после мержа | `2fb818d` (было `3cc1f2e`) |
| Брейн `master` | `7f0447c` |
| Пакет | `@aura/contracts` **v0.9.1**, тег запушен |

Оба репозитория сверены с origin: **`0 0`** — ничего не осталось висеть локально.
Проверено явно, потому что скрипт релиза умеет заминать неудачный пуш брейна.

В брейн ушли пока только шаги 003–004 — они были закоммичены на ветке раньше.
Шаги 005–010 копятся в слэйве и уедут одним коммитом при закрытии задачи, по
регламенту.

## Состояние

Код на `develop`, **в живом стеке не выполнялся ни разу**. Прод не трогали:
Coolify по-прежнему развёртывает старую ветку (`AURAT-0029`), так что мерж сам
по себе ничего не выкатил.

## Что проверять и в каком порядке — шаг 009

Кратко, по убыванию риска:

1. **Собрать образ.** Меняется тег git-зависимости; `AURAT-0034` на этом чуть не
   уронил прод.
2. **`GET /v1/attachments/:id`** — единственное место, не проверенное ничем:
   запрос идёт в маршрут Rails, в который этот сервис никогда не ходил.
3. Всё остальное — приём файла, отказ чужому, подстраховка опросом, дубль
   вебхука, пропавший файл.

На девайсе картинки не будет: рисование — половина `aura-app-manor`. Девайс
сегодня отвечает на другой вопрос — **не сломало ли выпущенное приложение
(v0.7.0) новое поле в ответе.**

## Не закрыто

Задача остаётся `in-progress`. Закрывать после проверки в маноре: тогда же
колонка BE строк 001–005 `AURAF-0011` переводится в `✓`, missus коммитится одним
коммитом и слэйв освобождается.
