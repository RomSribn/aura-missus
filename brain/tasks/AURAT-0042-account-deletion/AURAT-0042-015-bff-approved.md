# AURAT-0042-015 — Половина BFF принята владельцем

Дата: 2026-09-16
Владелец: «Принято» — проверка на `bff-dev` по списку из `014`

## Статус

**Половина BFF — done.** `aura-bff` `develop` @ `6698f1e`, `@aura/contracts`
**v0.19.0** (`4d6a4ef`).

**Задача AURAT-0042 в целом остаётся открытой**: половина приложения идёт в
`aura-app-manor` своей серией шагов `101`–`1xx` (на момент закрытия этой
половины — `111-app-execute`). Её спека (`105`) читает контракт ровно так, как
он сделан здесь: 204 / 409 `account_has_live_sessions` / 401 и WS-закрытие
`account_deleted`. Расхождений нет.

## Что получила половина приложения

- `DELETE /v1/me`: 204 и 204 на повтор; 409 `account_has_live_sessions`
  (`AccountDeletionRefusalCode`) — отвести к сессиям (Q1).
- После 204 любой другой маршрут отвечает 401 `ACCOUNT_DELETED`, WS
  закрывается 4401 с той же причиной. Выйти локально и больше ничего не
  звать — в том числе **не** `DELETE /v1/devices/:token`.
- Личность в Firebase удаляет сервер (D2), SMS для повторного входа не нужна.
- Экран подтверждения до нажатия говорит: денежный след остаётся
  обезличенным, оценки советникам остаются анонимными без текста (Q2),
  **остаток кошелька пропадает** (Q4).
- Веб-путь для Play и письма на почту закрывает оператор командой
  `account:erase` (D5), своего сервера для этого не нужно.

## Для текста политики (карта `AURAT-0040-013`)

Удаление не достаёт ночные дампы: в production они хранятся 30 дней, в
development срок не задан (`AURAS-0004` после `AURAD-0016`). При восстановлении
удаления повторяются по правилу в `AURAS-0004`. Не достаёт и строки `audits` у стойки
по уничтоженным перепискам: атрибуты есть, текстов нет. Строка про удаление
отдельной переписки уходит (D1).

## Что осталось за границей половины BFF

- `TECH-DEBT #28`: тексты последних ответов стойки в выполненных и упавших
  джобах вебхука в Redis.
- Если воркеры BullMQ уедут в отдельный процесс (`TECH-DEBT #3`), закрытие
  сокетов из джобы удаления придётся перенести.
