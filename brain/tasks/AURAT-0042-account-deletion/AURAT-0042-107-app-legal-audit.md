# AURAT-0042-107 — Сверка сбора данных на 2026-09-16: фактура для текста страниц

Дата: 2026-09-16
Пишет: `aura-app-manor` / `slave-0`
Метод: две независимые сверки, только чтение — приложение (`aura-app`
`develop` @ `506a318` + релизный манифест сборки 19) и BFF (`aura-bff`
`develop` @ `1a1003c`, контракты `v0.18.0`, `AURAS-0004`, утверждённая
`008` BFF). База сравнения — `AURAT-0040-006` (27.08), карта `AURAT-0040-013`.
Метки: **[код]** проверено в коде, **[док]** из brain, **[план]** — `008`/`009`
BFF, **[?]** — не установлено, как установить — указано.

## 1 · Что изменилось с 27.08

- **Имя ушло из Firebase в нашу базу** [код] (приложение `af901a9`, 08.09):
  `users.displayName`, обязательное, 2–64. Имена, заданные старыми сборками,
  остались на записях Firebase — их снимет `deleteUser` при удалении [план].
  **`AURAT-0040-012` и строка карты «дописать имя к Google в §05» устарели.**
- **Новые поля профиля** [код]: `email` (необязательный, не проверяется, ни
  для чего не используется), `birthDate` (**только дата**, без времени и места;
  никуда не отправляется — гороскоп-API нет), `marketingOptIn`, фото профиля.
- **Фото**: R2 `aura-user-media` (EU, приватный) **и копия байтами на контакте
  Chatwoot** — операторы видят лицо [код]. Перекодируется до 1024 px, EXIF
  снимается, **кроме случая, когда перекодирование упало** [код].
- **Отзывы** [код]: звёзды, текст до 400 (модерируется человеком), флаг
  anonymous; подпись — первое слово имени или `Anonymous`, видна **другим
  пользователям**.
- **Таро** [код]: дневная карта (в запросе часовой пояс IANA, не хранится),
  выбор позиции в раскладе.
- **Бронирование и Play** [код]: `purchaseToken`, `productId` от приложения,
  `orderId` от Google, `purchaseAccountId` (случайный UUID) уходит в Google.
- **Вложения** [код]: камера, галерея, любой файл, вставка из буфера; до
  25 МБ; записи голоса и видео в приложении нет (только готовые файлы).

## 2 · Что было и раньше, но в базу 27.08 не попало

- **Логи BFF содержат IP (`X-Forwarded-For`), User-Agent и FCM-токены в URL**
  [код, `app.module.ts:32-39` редактирует только `Authorization`].
  **Строка карты «убрать IP из §02» неверна.** Ротация 3×10 МБ — дни, не
  месяцы [док].
- **Операторы в Chatwoot видят телефон, Firebase UID и фото** контакта [код,
  `provisioning.service.ts:89-94`]. Имени, почты и даты рождения — нет.
- **Логи Chatwoot, вероятно, содержат тексты сообщений и телефоны**
  (фильтр параметров их не покрывает) [код upstream] — **[?]** `docker logs`
  на `chatwoot-rails`.
- **URL вложений Chatwoot публичные** — по нашему API вложение отдаётся только
  владельцу, по ссылке стойки — любому [док `AURAD-0011`].
- **Redis держит 1 000 / 5 000 последних джоб вебхука с текстом ответов
  операторов** без срока [код] — вне объёма BFF-удаления (`008`).
- **Push-токен отправляется, даже если человек запретил уведомления** [код].
- **Токен Firebase в URL WebSocket** (`?token=`) [код] — может оседать в
  логах прокси **[?]** (`--accesslog` Traefik).
- **Chatwoot шлёт агрегатную телеметрию** в Chatwoot Hub (счётчики, не
  персональные данные), `DISABLE_TELEMETRY` не задан [код upstream].

## 3 · Что в опубликованном тексте ложно сверх карты `AURAT-0040-013`

| Где | Утверждение | Как на деле |
|---|---|---|
| Privacy §01 | советники — независимые практики, отдельные контролёры | персоны над пулом операторов (`AURAD-0001`); решено писать честно (`106` #10) |
| Privacy §02 | «we do not ask for … your email» | Create Account спрашивает, необязательно |
| Privacy §02 | не названы почта, фото, согласие на маркетинг, отзывы, часовой пояс | собираются |
| Privacy §03 | «aggregated product statistics» | нет такой обработки |
| Privacy §05 | «a small number of our staff» | пул операторов (сотни, 24/7) видит переписку, телефон и фото; модераторы читают отзывы; не названы Cloudflare, Netlify, Resend |
| Privacy §08 | переносимость, исправление | механизма выгрузки нет (только вручную по запросу); телефон не исправить, имя не стереть кроме удаления |
| Privacy §09 | «attachments are served only to authenticated requests» | верно для нашего API, неверно для ссылок стойки |
| Privacy §09 | «access to production systems is … logged» | не подтверждено **[?]** |
| Privacy §10 | пуш «when a session is about to end» | такого пуша нет; пуши — только о новом сообщении |
| Privacy §10 | маркетинг: «every one carries a way to unsubscribe» | отправителя писем нет вовсе; отписки нет |
| Privacy §13 / Terms §15 | «tell you in the app before it takes effect» | механизма нет |
| Terms §02 / Privacy §11 | «by creating an account you confirm» 18+ | возраст нигде не спрашивается; есть только «By continuing you agree to Terms/Privacy» |
| Terms §06–07 | правила денег сессий не названы | 24 ч — полный возврат, меньше — половина; перенос до 4 ч; досрочное завершение сжигает остаток; $5 на первую сессию |
| Terms §07/§11/§12 | «unused credits may be refunded / we refund unused credits» | возврат в деньги только вручную через Play Console; при удалении аккаунта остаток сгорает (Q4) |
| Terms §09 | лицензия на контент — «to you and to the advisor you chose» | отзывы показываются другим пользователям с именем |
| Terms §11 | «advisors set their own hours» | персоны, присутствие по пулу |

**Карта `AURAT-0040-013` сама устарела в трёх строках:** «убрать дату
рождения» (теперь собирается — переписать), «дописать имя к Google в §05»
(имя ушло к нам), «убрать IP» (IP в логах есть).

**Три предложения «ЧЕГО НЕ ТРОГАТЬ» остаются верными**: логи сайта у Netlify
с IP; серверные логи есть (неверен только срок); рекламных cookie нет.

## 4 · Верно и сегодня

Нет аналитики, краш-репортинга, рекламных SDK и рекламного ID (проверено по
манифесту релизной сборки). Нет iOS и App Store. Карты и платёжные данные у
Google. Данные в ЕС: Hetzner (Хельсинки), R2 в EU-юрисдикции, Resend EU.
Бэкапы обеих баз — ежедневно, 30 дней, R2 `aura-backups`; вложения и аватары
не бэкапятся. `allowBackup=false` на Android. Пуш не несёт текста сообщения.

## 5 · Сопутствующие правки приложения (не текст политики)

- Edit Profile: **«Only your advisors see your name.»** — ложно: имя до
  стойки не доходит вовсе, зато видно другим в подписи отзыва
  (`screens/edit-profile/config/copy.ts:22`).
- Create Account: **согласие на маркетинг включено по умолчанию** — по GDPR
  заранее включённая галочка не является согласием
  (`screens/create-account/model/use-create-account.ts:25-29`).

## 6 · Не установлено — проверить в маноре до публикации

1. Логи `chatwoot-rails`: есть ли тексты сообщений и телефоны.
2. Traefik: включён ли access log (токен WS в URL).
3. Письма Resend операторам «conversation waiting»: цитируют ли текст.
4. Chatwoot → Integrations: включён ли перевод (Google Translate / OpenAI) —
   если да, это ещё один получатель текстов.
5. Публичен ли `avatar_url` контакта Chatwoot, как ссылки вложений.
6. Redis: пишется ли на диск и попадает ли в бэкап.

Дальше: развилки для владельца (где честное описание хуже правки продукта),
затем бриф.
