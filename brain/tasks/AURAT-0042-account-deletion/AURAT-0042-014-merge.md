# AURAT-0042-014 — Смержено в `develop`

Дата: 2026-09-16
Подтверждение владельца: «Да, пуш тега» и «Да, мёрж» (гейт, 2026-09-16)

## Что ушло

| Репозиторий | Что | Результат |
|---|---|---|
| `aura-contracts` | `main` `c19be95..4d6a4ef`, тег **`v0.19.0`** | запушено, тег виден в origin |
| `aura-bff` | `feature/AURAT-0042-account-deletion` (`e4029b9`) → `develop` | merge `6698f1e`, запушен `1a1003c..6698f1e` |
| `aura-missus` | — | не коммитится до закрытия задачи; шаги `004`–`014` и правка `AURAS-0004` лежат застейженными в слоте |

Перед мёржем `npm ci` в слоте с нуля поставил `@aura/contracts` 0.19.0 с
опубликованного тега по обновлённому `package-lock.json`, так что сборка
образа зависимость найдёт.

`wts-finish` освежил свободные слоты. `slave-3` остаётся на ветке для
пост-мёржевых шагов.

## Что это включило

Мёрж в `develop` деплоит окружение `development` (`bff-dev.aura-app.cc`,
`AURAD-0015`). На старте контейнера `prisma migrate deploy` применяет
`20260916120000_account_deletion` — только expand. Прод (`bff.aura-app.cc`)
не затронут: он обновится только релизом `develop → main`.

Дальше: проверка в маноре и на `bff-dev` по списку из `008` и `011`/`013`.
