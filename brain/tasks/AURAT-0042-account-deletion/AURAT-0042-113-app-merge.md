# AURAT-0042-113 — Половина приложения смержена в `develop`

Дата: 2026-09-16
Подтверждение владельца: «Да, мёрж» (гейт, 2026-09-16)

| Репозиторий | Что | Результат |
|---|---|---|
| `aura-app` | `feature/AURAT-0042-account-deletion` (`782b893`, `347c0bc`) → `develop` | merge **`6363671`**, запушен `506a318..6363671` |
| `aura-missus` | — | шаги `101`–`111` уже в `master` (`40baa56`); `112`+ ждут закрытия задачи |

Порядок «сервер первым» соблюдён: `aura-bff` `develop` = `6698f1e` с
удалением аккаунта влит раньше (BFF `014`).

`slave-0` остаётся на ветке для шагов после мёржа.

## Для манора

- **Мёрж меняет зависимости** (`@aura/contracts` `v0.18.0` → `v0.19.0`) —
  в маноре `npm install` перед сборкой (правило `AURAT-0060`).
- Нативной пересборки сверх обычной не требует; Metro — со сбросом кеша.
- Для internal testing нужен `versionCode 20` (сейчас `19`, как у сборки в
  Play) — отдельным `build:` коммитом в маноре, как в `AURAT-0068`.

## Что проверить (сборка `staging`, `bff-dev`)

Список — в `111`, раздел «Проверка в маноре». Сборка из слота с тем же кодом
уже стоит у владельца на Galaxy A50 (`112`), и кнопки приняты.

Дальше: подтверждение владельца → закрытие.
