# AURAT-0042-114 — Половина приложения принята владельцем

Дата: 2026-09-16
Владелец: «Принято» — проверка по списку из `113` на сборке `staging`
(`bff-dev`) на Galaxy A50

## Статус

**Половина приложения — done.** `aura-app` `develop` @ **`6363671`**
(`782b893` удаление, `347c0bc` кнопки), в origin. Половина BFF — done
(`015-bff-approved`, `aura-bff` `develop` @ `6698f1e`, контракты `v0.19.0`).

Шаг назван `accepted`, а не `approved`, намеренно: файл `*-approved.md` в
папке задачи — признак закрытой **задачи**, а AURAT-0042 целиком закрыть
нельзя, пока не опубликованы страницы.

## Что остаётся открытым по задаче

Всё — действия владельца или других задач, кода в `aura-app` не требует:

1. **`AURAT-0072`** (`aura-bff-manor`) — имя пользователя на контакте
   Chatwoot. Текст политики уже говорит, что операторы видят имя.
2. **Релиз в прод:** `aura-bff` `develop → main` с удалением и `AURAT-0072`;
   сборка приложения в Play (`versionCode 20`, `npm install` в маноре —
   контракты сменились).
3. **Cloudflare Email Routing** для `privacy@` и `support@aura-app.cc`,
   проверочное письмо.
4. **Шесть проверок в маноре** из `107` §6 и правка брифа по их итогам.
5. **Бриф → Claude Design → Netlify**, затем privacy URL, URL удаления и Data
   safety в Play Console (`AURAT-0042-claude-design-brief.md`, часть 2).
6. **Процедура проверки личности** для запросов удаления по почте — до
   первого настоящего письма.

Сверено с принятой половиной BFF: `015` называет срок дампов 30 дней для
production (после `AURAD-0016`), бриф говорит то же.

## Хвосты, записанные по дороге

- Копии исходящих файлов в кеше приложения при удалении не чистятся (`111`).
- «Only your advisors see your name» в Edit Profile — «only» неверно (`108`).
- Лендинг `/` с бейджем App Store и без ссылки на Google Play (бриф, часть 2).

Дальше: release `slave-0`.
