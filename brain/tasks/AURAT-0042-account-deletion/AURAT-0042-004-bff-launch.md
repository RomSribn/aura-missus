# AURAT-0042-004 — Половина BFF запущена в работу

Дата: 2026-09-16
Пишет: `aura-bff-manor` / `slave-3`
Ветка: `feature/AURAT-0042-account-deletion` в `aura-bff` (от `develop` @ `1a1003c`)
и в `aura-missus` (от `master` @ `5a6b4df`)

## Запрос владельца

> «запускай AURAT-0042 в slave-3»

## Фон, переданный из `aura-app-manor`

- ID уже выдан, новый не заводится; шаги продолжаются с `004`.
- Здесь исполняется **только половина BFF**. Половина приложения делается в
  `aura-app-manor` отдельно и сейчас не запущена; контракт между ними — через
  `@aura/contracts`.
- С 14.09 (`AURAD-0015`, `AURAS-0004` → Environments) мёрж в `develop` деплоится
  в окружение `development` (`bff-dev.aura-app.cc`); прод (`bff.aura-app.cc`)
  обновляется только релизом `develop → main` с подтверждения владельца.
- Миграции применяются `prisma migrate deploy` на старте контейнера, поэтому
  **деструктивная миграция идёт через expand/contract**.
- Мёрж — как обычно, через подтверждение владельца.

## Дальше

Сверить `001`–`003` с текущим `develop` (с 28.08 в базу въехали отзывы,
фотография профиля и другое), собрать решения, появившиеся после `003`, и
написать спеку половины BFF на утверждение.
