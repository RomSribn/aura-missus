# AURAT-0069-009 — задача закрыта

Дата: 2026-09-14 · slave-2 · `feature/AURAT-0069-bff-tarot-keys-jpg`

Статус: **done**. Владелец проверил на устройстве после деплоя: «всё ок».

## Что уехало

| Репозиторий | Что |
|---|---|
| `aura-bff` `develop` | `daf42bd` → **`1fa9fbe`** (коммит `fe3025a`): 78 `imageKey` → `.jpg`, фикстуры и примеры таро → `.jpg` |
| `aura-missus` | один коммит при закрытии: шаги `002`–`009`, манифест и upload-скрипт `AURAD-0012` под JPEG, поправка в решение |

## Уточнение к `008` — куда деплоится `develop`

Пуш в `develop` уходит на **`bff-dev.aura-app.cc`** (Coolify `development`,
target `staging` в приложении — `AURAT-0068`). Домен `bff.aura-app.cc` был
временным вторым адресом того же контейнера и снят — после мёржа он отвечал
Traefik-овским 503 `no available server`, к этой задаче отношения не имеет.
`bff-dev` после деплоя: `/health` 200, `/health/ready` — база и Redis `true`.

`AURAS-0004` всё ещё описывает `bff.aura-app.cc` как прод, а `develop` — как
продовую ветку; обновлять его — не эта задача.

## Осталось названным, но не сделанным

- **Приложение**: комментарий о пропорции арта в
  `aura-app/src/entities/tarot/config/card.ts` устарел (413×712); новый арт
  1,467 шире рамки 1,534, `cover` режет бока. За `aura-app-manor` и дизайнером.
- **Порядок замены арта**: в поправке `AURAD-0012` записано правило «старые
  объекты удаляются только после деплоя, переставившего ключи».
