# AURAT-0066-001 — что попросил владелец

Дата: 2026-09-08
Слот: slave-0, ветка `feature/AURAT-0066-app-write-review`
Манор: `aura-app-manor` (приложение). Серверная половина — отдельной задачей
в `aura-bff-manor`.

## Дословно

> обновил дизайн /Volumes/Work/personal/design_handoff_aura нужно реализовать
> функционал оставления коментариев в проекте, так же обнови design handoff.
> Продумай реализацию на фронте и бекенде

Со скриншотом сопроводительной записки дизайнера к дропу.

## Что приехало в дропе (2026-09-08)

- `REVIEWS.md` — спека: модель «двух скоростей», право на отзыв, три точки
  входа, разбор формы, тост, модель данных с требованиями к бэкенду, раздел
  «почему нет статусов», моторика, чек-лист.
- Экраны 37–41: `37-review-locked`, `38-review-cta`, `39-write-review`,
  `40-write-review-filled`, `41-review-sent` (всего в дропе 41 против наших 36).
- RN-порты: `rn/AuraStarPicker.tsx`, `rn/AuraWriteReview.tsx`,
  `rn/AuraReviewPrompt.tsx` (+ `AuraToast` внутри последнего).
- `prototype/reviews.jsx`; секция «08 · Reviewing an advisor» в галерее и в
  печатной версии; §15c и состояния `reviews` / `toast` в `README.md`.

## Две оговорки по интейку дропа (от диспетчера)

1. Файл дропа `CLAUDE.md` — это МОТОРИКА И ИКОНКИ; при интейке он
   переименовывается в `MOTION_AND_ICONS.md` (вложенный `CLAUDE.md`
   подхватился бы как инструкции; источников правил ровно два).
2. В дропе нет `CHAT_ATTACHMENTS.md` — он есть только в проектной копии.
   При синхронизации не удалять: дроп неполон по составу, а не урезан.

## Границы

«Продумать реализацию на бекенде» = спроектировать контракт и записать его так,
чтобы `aura-bff-manor` взял в работу. Код BFF отсюда не пишется (прецедент
AURAT-0064 / AURAD-0013). Слот-правила в силе: стек не поднимать, только
линт / тайпчек / тесты / сборка.
