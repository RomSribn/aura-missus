# AURAT-0066-009 — влито в манор

Дата: 2026-09-08

## Порядок оказался правильным

Серверная половина пришла **первой**, как и требовал план:
`aura-bff` `develop` = `daf42bd` (`Merge feature/AURAT-0067-bff-write-review`),
и ветка синхронна с origin — то есть по `AURAS-0004` это уже деплой.

Сверено перед мержем, а не на слово:

- контракты в BFF — `#v0.18.0`, тот же тег, что у приложения;
- `@@unique([userId, advisorId])` в `advisor_reviews`, `sessionId` **без**
  `@unique`, `quoteStatus` с дефолтом `PUBLISHED`;
- миграция `20260908180000_review_writing`;
- модуль `src/modules/reviews` с обоими роутами и отказом
  `review_not_eligible`.

То есть обе развилки, ради которых писался бриф, сервер прошёл так, как
просили: ключ уникальности переехал, а `published` остался флагом строки.

## Что влито

- `aura-app` `develop` = **`cf08f8d`** (`Merge feature/AURAT-0066-app-write-review`),
  отправлено в origin.
- Workspace (`b7ab79a`) — синхронизация хендоффа с дропом.
- Brain (`4515679`) — спека, решение и обе половины. **В origin не отправлен.**

## Проверено в маноре

После мержа, меняющего зависимости, `npm install` обязателен — правило
записано в `AURAT-0060`. Сделан: пакет в маноре `0.18.0`.
`tsc` и **784 теста** на слитом дереве — чисто.

## Что дальше

Проверка на устройстве владельцем. Нативной пересборки этот мерж **не
требует** — ни иконок, ни сплэш-экрана, ни новых нативных модулей; хватит
перезапуска бандлера. Но `npm install` уже изменил `node_modules`, так что
Metro нужно поднимать со сбросом кеша.

Слот остаётся за задачей до приёмки.
