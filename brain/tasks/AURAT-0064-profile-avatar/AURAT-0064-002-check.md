# AURAT-0064-002 — Что уже есть

Дата: 2026-09-08
Слейв: `aura-bff-manor/slave-2`, ветка `feature/AURAT-0064-profile-avatar`
(от `develop` = `d41aa5d`, missus от `ac28711`).

## В папке задачи

Только `001-initial.md` — заглушка от 08.09, вынесенная из `AURAT-0062`
решением владельца D4. Работа не начиналась: ни спеки, ни кода, ни ветки до
сегодняшнего дня не было.

## Найдено по соседству

| Артефакт | Чем полезен |
|---|---|
| `AURAF-0015`, строка `009` | Дом этой задачи. Строка стоит `UI ✗ / BE ✗`, в `NOT in scope` записано, почему кнопку не рисовали |
| `AURAT-0063` / `AURAT-0062` | Образец двухрепозиторной пары: две задачи, два мержа, порядок «сервер первым». Спека `0063` — образец формы для этой |
| `AURAD-0011` | Личные файлы наружу ссылкой не выдаём. Плюс поправка 21.08: вложения хранит Chatwoot, BFF только проксирует |
| `AURAD-0012` | Публичный `aura-assets`, ключ в базе, и правило «живой ключ не перезаписывают» |
| `AURAT-0031` / `AURAT-0032` | Рельса вложений: приём (сниффер, потолки) и отправка байтов в Chatwoot |
| `AURAT-0033` | Аватары советников в `aura-assets`, префикс `avatar/` уже занят ими |
| `AURAT-0042` | Удаление аккаунта — обязано будет удалять и объект в хранилище |
| `AURAS-0004` | Мёрж в `develop` = боевой деплой. Отсюда весь вопрос о порядке половин |

## Занятость

`slave-1` держит `AURAT-0058` (отзывы советников, не влита) — пересечений с этой
задачей нет: разные таблицы, разные модули, разные маршруты.

Дальше: `003-understand`.
