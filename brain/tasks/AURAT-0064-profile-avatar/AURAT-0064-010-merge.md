# AURAT-0064-010 — Мёрж

Дата: 2026-09-08

Ветка `feature/AURAT-0064-profile-avatar` = `a2c2578`, влита в `develop`
с разрешения владельца после просмотра дифа.

**`aura-bff` `develop` = `07297b7`**, запушено в `origin`. Контракты —
`v0.17.0` (`7a8d71a`). Слейв остаётся на ветке для послемёржевых шагов;
missus не коммичен — по дисциплине это один коммит при закрытии задачи.

Idle-слоты `slave-3`, `slave-4`, `slave-5` обновлены до нового master.

Пуш в `develop` — боевой деплой (`AURAS-0004`). Переменные `AVATAR_STORAGE_*`
владелец завёл в Coolify **до** мержа, поэтому сборка должна подняться. Если бы
их не было, `validateEnv` не прошёл бы, контейнер не стартовал, а предыдущая
версия продолжила обслуживать — деплой, который не взлетел, а не выключенный
прод.

Один пуш пересобирает **обе** службы, BFF и Chatwoot: свойство площадки, не
этой задачи.

Дальше — проверка на живом стеке.
