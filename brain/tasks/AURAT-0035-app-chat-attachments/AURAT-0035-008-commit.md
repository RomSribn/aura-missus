# AURAT-0035-008 — Закоммичено, не замержено

Дата: 2026-08-21
Коммит: `f7362d8` «feat(chat): attachments, in both directions»

Приём и отправка легли **одним коммитом вместе с `AURAT-0036`**. Разделить не
получилось бы честно: отправка построена на приёме — отправленный файл рисуют те
же рендереры, несёт та же вью-модель, показывает тот же баббл. Первый коммит из
двух не собрался бы.

## Гейты на момент коммита

`tsc` — 0 ошибок, `lint` — чисто, `jest` — 60 наборов, 373 теста.

## Мёрж

**Отложен владельцем** (2026-08-21): на прямой вопрос про `wts-finish` ответ —
«нет, подожди». Ветка остаётся в `slave-0`, слэйв не освобождаётся.

Вместо мёржа собран релизный AAB для проверки на устройстве — см.
`AURAT-0036-007`.
