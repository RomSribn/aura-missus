# AURAT-0035-002 — Что уже есть в брейне и в слотах

Дата: 2026-08-21

## Брейн синхронизирован перед проверкой

Клон missus в этом маноре отставал на **19 коммитов** (`ea31ee2` → `7f0447c`).
Проверять состояние по нему до `pull` значило бы не увидеть ни `AURAF-0011`, ни
`AURAD-0011` — то есть завести дубль уже описанной работы. Подтянуто
fast-forward.

## Найдено

| Артефакт | Статус | Что даёт этой задаче |
|---|---|---|
| `AURAF-0011-chat-attachments` | draft, строки 001–009 все `✗` | Скоуп и приёмка. Таблица **устарела**: BFF-половина 001–005 закрыта, но `BE` там ещё `✗` |
| `AURAD-0011-attachment-delivery` | accepted | Транспорт байтов решён: BFF проксирует, адрес хранилища приложению не выдаётся ни в каком виде |
| `AURAT-0031-chat-attachments-receive` | шаги 001–004 на `master` | BFF-половина. Шаги 005–011 незамёржены (ветка слэйва bff-манора) |
| `AURAT-0032-chat-attachments-send` | 001, не начата | Обратное направление. **BFF-половины у неё тоже нет** — `SendMessageRequest.content` всё ещё `.min(1)`, эндпоинта загрузки нет |

## Состояние слотов (`active-work.md` расходится с реальностью)

- `slave-0` — detached HEAD, свободен.
- `slave-1` — на `feature/AURAT-0028-bff-url-and-env-tooling`. В `active-work.md`
  числится `busy`, но задача замержена (`aura-app` @ `a66a8c0`) — слот не
  освободили после мёржа.

Исполнять `AURAT-0035` — в `slave-0`.

## Дубля нет

Задачи на показ вложений в приложении в брейне нет: `Glob` по `brain/tasks/` даёт
только `AURAT-0031` (BFF) и `AURAT-0032` (отправка).
