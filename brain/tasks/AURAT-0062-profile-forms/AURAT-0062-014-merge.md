# AURAT-0062-014 — Влито

Дата: 2026-09-08

`wts-finish slave-0` по явному разрешению владельца.

- Ветка: `feature/AURAT-0062-profile-forms` = `e03f424` (четыре коммита).
- Манор `develop` = **`1043ed8`** (`Merge feature/AURAT-0062-profile-forms`),
  пуш прошёл: `5eb0341..1043ed8`, `origin/develop` совпадает.
- Слейв остался на ветке под пост-мержевые доки, как и положено.
- Слот `slave-1` (`AURAT-0056`) не тронут; свободные слоты обновлены скриптом.

Серверная половина была влита раньше (`aura-bff` `develop` = `d41aa5d`), так
что порядок из решения D1 соблюдён: маршрут существовал до того, как экран,
который в него ходит, попал в `develop`.

## Что теперь нужно в маноре

`package.json` изменился (`@aura/contracts` → `v0.16.0`), а установленным там
остался `v0.15.0`. **`npm install` и перезапуск Metro со сбросом кеша** —
правило, записанное в `AURAT-0060` ровно по этому поводу. Без этого бандлер
соберёт код, который импортирует из пакета то, чего в установленной версии нет.

## Что осталось открытым после этой задачи

- **`AURAF-0015-004`** — знак зодиака из введённой даты. Клиентская часть
  сделана (после записи даты прогноз перечитывается принудительно), но
  `GET /v1/horoscope` **не построен**: это `AURAT-0039`, чья серверная половина
  так и не начата. Секция оживёт в день появления маршрута без правок здесь.
- **`AURAT-0064`** — фотография профиля.
