# AURAT-0063-012 — Принято владельцем

Дата: 2026-09-08
Статус задачи: **done**

Владелец в ходе просмотра поднял один вопрос — «у нас что, теперь профайл
таблица отдельно от юзера?» — и по нему принято решение свернуть профиль в
строку пользователя (`009`). После этого: «давай тогда проверку ендпоинтов и
релизь с пушем».

Проверка проведена и записана в `011-manor-verified.md`; разрешение на мёрж и
пуш — оттуда же.

## Что построено

Четыре колонки в `users`, модуль `profile` с `GET`/`PATCH /v1/me`, серверная
валидация, `@aura/contracts` v0.16.0.

- `aura-bff` `develop` = `d41aa5d`, в `origin`.
- `aura-contracts` `main` = `250f61e`, тег `v0.16.0` в `origin`.
- Прод получил это тем же мержем (`AURAS-0004`).

## Что осталось за пределами

- **`AURAT-0062`** (клиент) — разблокирована, мёржится следующей.
- **`AURAT-0039`** — `GET /v1/horoscope` по-прежнему не построен; в `TECH-DEBT`
  #27 запись оставлена и переписана под то, что от неё осталось.
- **`AURAT-0064`** — аватар.
