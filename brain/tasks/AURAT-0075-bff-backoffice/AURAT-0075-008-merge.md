# AURAT-0075-008 — Смержено в develop

Дата: 2026-09-17

Владелец смотрел UI на статическом превью из настоящих шаблонов (живой
сервер в слоте запрещён), сказал «комить и мерж в девелоп» и подтвердил мёрж
вопросом «Merge → Да, мёрж».

- Коммит на ветке: `ca1a2b2` `feat(backoffice): AURAT-0075 — бэкофис:
  модерация отзывов и удалённые аккаунты`.
- `wts-finish slave-2`: `aura-bff` `develop` `abe2039` → **`d3c8ec8`**
  (`Merge feature/AURAT-0075-bff-backoffice`), запушено в origin. Манор
  `develop` совпадает с `origin/develop`. Слоты 3–5 обновлены.
- Brain манора перед мёржем подтянут fast-forward до `origin/master`
  (`7ef788e`, 9 коммитов app-манора). Мёрж brain — «Already up to date»:
  документы задачи не закоммичены, как требует skill. Push brain выдал
  предупреждение: за время мёржа app-манор запушил `3ddee3b`, и манор отстал на
  1. Наших коммитов там нет, ничего не потеряно.
- `slave-2` остаётся на ветке для документов после мёржа.

## Что проверить на bff-dev

1. Деплой `d3c8ec8` прошёл, в логе миграция
   `20260917120000_backoffice_operators`.
2. Терминал контейнера BFF в Coolify:
   `node dist/backoffice-operators.js add <login>` — пароль печатается один раз.
3. `https://bff-dev.aura-app.cc/backoffice` — вход, выход, неверный пароль.
4. Очередь отзывов: опубликовать или отклонить живой `PENDING`, увидеть в
   приложении. Скрыть и вернуть отзыв — число на профиле меняется и
   возвращается.
5. «Удаления» совпадают с `account_erasures`.
6. В логе BFF нет `cookie`.
7. Вид на телефоне.
