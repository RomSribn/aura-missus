# AURAT-0075-009 — Принято

Дата: 2026-09-17
Статус: **done**

Владелец проверил бэкофис на `bff-dev` после мёржа (`008`) и ответил
«принято». Что именно он прогонял, в ответе не сказано; список проверок для
манора — в `008`.

## Где что

- `aura-bff` `develop` @ `d3c8ec8` (коммит `ca1a2b2`), задеплоено на
  `bff-dev`: `GET /backoffice/login` отвечает 200 (до деплоя был 404).
- Миграция `20260917120000_backoffice_operators` едет сама при старте
  контейнера.
- Brain: спека `005` — done; `AURAF-0016-013/014` — `BE` ✓; дополнение в
  `AURAD-0014`; в `AURAF-0014-007` упомянут бэкофис.

## Осталось за пределами задачи

- **Релиз в `main`.** В проде бэкофиса нет. Новых переменных Coolify не
  нужно. После релиза на проде нужно завести учётку:
  `node dist/backoffice-operators.js add <login>` в терминале контейнера BFF.
- Не сделано по спеке, осознанно: 2FA, роли, смена и восстановление пароля на
  странице, действия на вкладке «Удаления» (удаление по просьбе и повтор из
  журнала остаются командами `account:erase`).
