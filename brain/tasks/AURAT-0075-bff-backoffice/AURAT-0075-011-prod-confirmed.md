# AURAT-0075-011 — На проде подтверждено владельцем

Дата: 2026-09-17
Статус: **done — в проде**

После релиза (`010`) владелец сам завёл учётку на проде и ответил: «все
работает». Из сессии слота вход и модерация на проде не проверялись — это
его проверка.

## Итог задачи

- `aura-bff` `main` = `develop` = `d3c8ec8`, `https://bff.aura-app.cc/backoffice`.
- Учётки на проде заводятся в терминале контейнера production `aura-bff`:
  `node dist/backoffice-operators.js add|reset|remove|list`.
- Эксплуатация записана в `AURAS-0004`:
  - бэкофис на хосте BFF и учётки;
  - что проверить после восстановления бэкапа — учётки возвращаются к
    состоянию дампа, а «Не завершённые» удаления видны на вкладке
    «Удаления»;
  - две строки в *When it breaks*.
