# AURAT-0067-006 — утверждение спеки

Дата: 2026-09-08

**Спека одобрена, приступать.** Три вопроса закрыты — все три рекомендации
приняты без правок:

- **Q1 · Имя автора при пустом `displayName`** → писать `'Anonymous'`, флаг
  `anonymous` оставлять тем, что прислал человек. Байлайн выходит «Anonymous»
  без «Verified client», и форма, которую приложение уже показало, доходит
  до конца.
- **Q2 · Тег `v0.18.0`** → **пушить перед мержем**. Работаю против локальной
  сборки пакета, `package.json` перевожу на `#v0.18.0`; отдельный явный вопрос
  про `git push` тега в `origin` — перед мержем в `develop`, вместе с вопросом
  о самом мерже.
- **Q3 · `sessionId`** → проверять только форму (`^[A-Za-z0-9_-]{1,64}$`), не
  существование; не прошло — NULL, отказа нет.

Дальше: `007-execute` — код на `feature/AURAT-0067-bff-write-review`,
без коммита, до ревью в IDE.
