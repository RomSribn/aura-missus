# AURAT-0067-009 — мёрж

Дата: 2026-09-08

`wts-finish slave-2` по явному разрешению владельца в том же ходе.

- ветка: `feature/AURAT-0067-bff-write-review`, один коммит `84022ab`;
- манор `aura-bff` `develop`: `85806af` → **`084a2f8`**, **запушено в origin**
  (по `AURAS-0004` пуш в `develop` = деплой);
- контракты: тег `v0.18.0` (`c19be95`) владелец запушил до мержа —
  `npm install` в слоте это подтвердил, ворота прогнаны против пакета из origin;
- missus — «Already up to date», как и положено: документы задачи коммитятся
  одним коммитом при закрытии;
- простаивавшие слоты 3–5 обновлены на новый master.

Слот остаётся на ветке для послемержевых шагов.

## Что проверять в маноре (живьём, чего слот не мог)

```bash
docker compose up --build          # прогонит prisma migrate deploy
npm run prisma:seed                # дважды: строк должно остаться 42
```

1. **Миграция** на 42 засеянных строках: снятие `advisor_reviews_sessionId_key`,
   `UNIQUE (userId, advisorId)` с NULL-ами, `DEFAULT 'PUBLISHED'` — все 42
   строки должны стать `PUBLISHED` без бэкфилла.
2. **Сид дважды** — 42 строки, счётчики советников не удвоились.
3. **`PUT /v1/advisors/:id/review`** живьём: без переписки → 403; после своего
   сообщения → 200 и пересчитанные `ratingTenths`/`reviewsCount` в `advisors`.
4. **Повтор** — одна строка на пару, счётчик не вырос (это и есть проверка
   нового уникального индекса).
5. **Скрипты**: `npm run reviews:pending`, затем `reviews:publish -- <id>` и
   `reviews:decline -- <id>` — после publish цитата появляется в
   `GET /v1/advisors`, после decline **не появляется**, и оба раза числа
   советника не двигаются.
