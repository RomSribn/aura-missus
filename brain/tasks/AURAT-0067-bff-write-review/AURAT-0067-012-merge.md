# AURAT-0067-012 — второй мёрж: вывод засеянных отзывов

Дата: 2026-09-08 · по прямому указанию владельца («пушай сразу в дев»)

- коммит `8515fad` на ветке, `wts-finish slave-2`;
- манор `aura-bff` `develop`: `084a2f8` → **`daf42bd`**, запушено в origin
  (`AURAS-0004`: пуш в `develop` = деплой, значит миграция удаления приедет
  и на прод);
- missus по-прежнему «Already up to date» — документы задачи одним коммитом
  при закрытии;
- слоты 3–5 обновлены.

## Что произойдёт при деплое

1. `prisma migrate deploy` применит `drop_seeded_reviews`: 42 строки
   `source = 'SEED'` удаляются, счётчики советников пересчитываются по
   оставшимся (то есть в ноль).
2. `node dist/seed.js` (бежит на каждом старте) больше **не** пишет отзывы и
   **не** трогает `ratingTenths` / `reviewsCount` у существующих советников.
3. `GET /v1/advisors` начнёт отдавать `reviews: []`, `0`, `0`; приложение по
   `AURAF-0014-005` перестанет рисовать секцию Reviews, звёзды и счётчик.

## Что проверить живьём

- миграция прошла, в `advisor_reviews` пусто, у всех восьмерых `0 / 0`;
- оставить настоящий отзыв → числа появились → **перезапустить контейнер** →
  числа на месте (проверка исправленного затирания сидом);
- `npm run prisma:seed` дважды — отзывы не возвращаются;
- очередь модерации на живом отзыве: `reviews:pending` → `publish` / `decline`.
