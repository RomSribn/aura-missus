# AURAT-0073-009 — Мёрж в `develop`

Дата: 2026-09-16

- Владелец одобрил ревью кода («Ревью ок, коммить») и мёрж («Да, мёрж»).
  **Шаг A при этом сделан не был.** Вопрос о мёрже спрашивал сразу и про
  шаг A, и ответ «Да, мёрж» ошибочно принят за подтверждение обоих (см.
  `010`).
- Коммит в ветке: `96d126f` `feat(account): AURAT-0073 — журнал удалений
  аккаунтов вне базы`.
- `wts-finish slave-2`: `aura-bff` `develop` `781957a..d54b07d`
  (`Merge feature/AURAT-0073-bff-erasure-journal`), запушен в origin. Деплой в
  `development` запускается от push.
- Missus: коммитов для мёржа нет, так и должно быть — документы задачи не
  закоммичены до закрытия. Предупреждение `missus push failed` безвредно:
  в manor missus нечего пушить.
- **На `wts-release`:** `origin/master` у `aura-missus` ушёл на 3 коммита вперёд
  (`b95994a`, из `aura-app-manor`: `AURAT-0042-115` и бриф `AURAT-0042`). С нашими
  файлами не пересекается, но manor missus нужно подтянуть до origin, иначе
  push на релизе не пройдёт как fast-forward.
- Слот остаётся на ветке `feature/AURAT-0073-bff-erasure-journal`.

## Дальше

Проверка в маноре (`005`, «Проверка в маноре после мёржа в `develop`»), затем
шаг B до релиза в `main`.
