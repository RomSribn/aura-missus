# AURAT-0073-017 — Принято

Дата: 2026-09-16

Владелец принял задачу («Принято») после проверки на `development` (`016`).

## Итог

- `aura-bff` `develop`: `96d126f` (фича), `d54b07d` (мёрж). Сейчас на dev
  работает `abe2039`: поверх лёг `AURAT-0074`, наш код в нём.
- Журнал удалений в R2 (`aura-erasure-journal`): запись первым шагом джобы
  `account-erasure`, `source` в `account_erasures`, `account:erase --
  --journal` для повтора после восстановления, правило бакета 45 дней.
- Проверено на `development`: удаление из приложения и оператором, записи в
  журнале, восстановление бэкапа, снятого до удаления, в отдельную базу,
  повтор `--journal`, финальные проходы, удаление в Firebase.

## Не сделано в этой задаче (осознанно)

- **Шаг B — до релиза в `main`:** `aura-erasure-journal-prod` (EU), правило
  45 дней, токен только на этот бакет, проверка токена, четыре
  `ERASURE_JOURNAL_*` в Coolify `production` **без** Build Variable. Без них
  прод после релиза не стартует: rolling update снимает старый контейнер
  сразу (`013`).
- Релиз в `main` — отдельное решение владельца (merge gate).
- Предложено, не делалось: поправить в `AURAS-0004` фразу про rolling update
  («the new container starts while the old one serves»). Без healthcheck
  старый контейнер снимается через секунду, и новый, упавший на старте,
  оставляет окружение без BFF (`012`–`013`).
- По желанию: `docker image rm` образа `d54b07d` на сервере. В нём
  `ARG ERASURE_JOURNAL_*` с отозванным секретом (`014`, `016`).

## Уроки процесса

- Вопрос о мёрже не объединять с вопросом «сделан ли шаг владельца»: ответ
  «Да, мёрж» был принят за подтверждение обоих (`010`).
- Для проверки восстановления дамп снимать **после** создания тестового
  аккаунта и **до** удаления (`016`).
