# AURAT-0073-016 — Проверка на `development` (провела сессия по SSH)

Дата: 2026-09-16
Владелец: «сделай это все сам». Доступ: `ssh root@37.27.199.90` (`AURAS-0004`).
Секреты не выводились: ключи R2 и Firebase брались из окружения dev-контейнера
внутри него самого.

## 0. Окружение

- Dev BFF `mtnsnawmogikfwm0uvl1g1yc-180104856263`, образ `d54b07d`; dev Postgres
  `mcsybichhmrfwclma4pbzgnr` (хост из `DATABASE_URL` контейнера). На нём только
  `aura_bff`, базы `chatwoot` нет, значит это не прод.
- В контейнере есть все четыре `ERASURE_JOURNAL_*`; миграция
  `20260916200000_account_erasure_source` применена.
- В истории образа остались `ARG ERASURE_JOURNAL_*`: последний деплой взял
  образ сборки 17:56. Там **старый, отозванный** секрет, нового секрета в
  истории нет. Образ очистится при следующей пересборке.

## Тестовые аккаунты

Настоящих аккаунтов dev не трогал. Три одноразовых пользователя Firebase
(`aurat-0073-probe-*`): custom token через Admin SDK dev-контейнера, обмен на
ID token через Identity Toolkit (API-ключ Android-приложения), затем
`GET /v1/me` на `bff-dev`, который заводит строку.

| Аккаунт | userId | Как удалён |
|---|---|---|
| probe-app (1) | `cmu4evd9c000001ox23gws911` | `DELETE /v1/me`, 18:05:23Z, **до** дампа |
| probe-app2 (2) | `cmu4ewfjn000201oxmfhy42bz` | `DELETE /v1/me`, 18:06:24Z, после дампа |
| probe-op (3) | `cmu4ewfsd000301oxwg6xj7cj` | `account-erasure.js --user`, 18:06:26Z, после дампа |

Первая попытка восстановления была с дампом, снятым **до** создания аккаунта
1, поэтому проверять на ней было нечего. База и дамп удалены, шаги повторены
в правильном порядке: аккаунты 2 и 3 созданы → дамп 18:06:14Z → удаление.

## Результаты

**Маршрут:** `DELETE /v1/me` → 204; тот же токен на `GET /v1/me` → 401
`account_deleted`; повторный `DELETE` → 204.

**Журнал** (читался изнутри контейнера), три объекта, у каждого
`application/json` и `Expiration … rule-id="expire-45d"` (+45 дней):
```
erasures/2026-09-16T18:05:23.449Z_cmu4evd9c000001ox23gws911.json  {"userId":…,"requestedAt":…,"source":"app"}
erasures/2026-09-16T18:06:24.083Z_cmu4ewfjn000201oxmfhy42bz.json  {…,"source":"app"}
erasures/2026-09-16T18:06:26.654Z_cmu4ewfsd000301oxwg6xj7cj.json  {…,"source":"operator"}
```
Записи появились через секунды. Операторскую пишет работающий сервис.
`account_erasures.source`: `APP`, `APP`, `OPERATOR`.

**Восстановление** дампа 18:06:14Z в отдельную базу `restore_0073` (владелец
`aura`, `pg_restore` exit 0, 16 таблиц): аккаунты 2 и 3 снова живые, аккаунт
1 уже надгробие. То есть именно та проблема, ради которой задача.

**`--journal` по `restore_0073`** (`DATABASE_URL` подменён только для этой
команды):
```
cmu4evd9c000001ox23gws911 was already erased
cmu4ewfjn000201oxmfhy42bz erased
cmu4ewfsd000301oxwg6xj7cj erased
3 in the journal: 2 erased, 1 already erased, 0 not in this database, 0 refused, 0 failed
exit=0
```
После прогона в `restore_0073` все три надгробия с очищенным профилем. Второй
прогон: 3 × `was already erased`, exit 0. В живой `aura_bff` число удалений не
изменилось (6).

**Уборка:** `restore_0073` удалена, дамп из `/tmp` контейнера удалён.

## Финальный проход (проверено в 18:13Z)

| userId | source | requestedAt | completedAt | ссылки очищены |
|---|---|---|---|---|
| `cmu4evd9c000001ox23gws911` | APP | 18:05:23 | 18:10:53 | да |
| `cmu4ewfjn000201oxmfhy42bz` | APP | 18:06:24 | 18:11:54 | да |
| `cmu4ewfsd000301oxwg6xj7cj` | OPERATOR | 18:06:26 | 18:11:56 | да |

- Все три пользователя `aurat-0073-probe-*` удалены в Firebase
  (`auth/user-not-found`).
- В логе BFF с момента старта нет строк уровня warn или error, нет
  `erasure journal write failed`, есть 3 × `account erasure completed`.

## Попутно: перезапуск в 18:08:44Z

В 18:08:44Z dev перезапустился на `abe2039`: slave-3 смержил `AURAT-0074` в
`develop` поверх нашего `d54b07d`. Это не падение (`RestartCount=0`).
Получилась живая иллюстрация задачи:
- строки `account erased` для удалений 18:05–18:06 **пропали** вместе со
  старым контейнером;
- записи в журнале на месте, а отложенные финальные проходы пережили деплой в
  Redis и завершились уже на `abe2039`. Значит, проверка покрывает и сочетание с
  `AURAT-0074`.

Образ `abe2039` собран без `ARG ERASURE_JOURNAL_*`. Отозванный секрет остался
только в старом образе `d54b07d` на сервере; его можно удалить
`docker image rm`, но секрет уже ничего не открывает.

## Итог

Всё из «Проверки в маноре» (`005`) выполнено: запись в журнал после удаления
из приложения и оператором, восстановление бэкапа, снятого до удаления,
возвращает аккаунты, `--journal` их снова стирает, повторный прогон
безопасен. Замечаний по коду нет.

Осталось до релиза в `main`: шаг B (`aura-erasure-journal-prod`, токен,
правило 45 дней, переменные в Coolify `production` **без** Build Variable).
