# AURAT-0073-006 — Спека утверждена

Дата: 2026-09-16

Владелец ответил на три вопроса:

| Вопрос | Ответ |
|---|---|
| Q1 — где хранить журнал | «Отдельный бакет (Recommended)»: `aura-erasure-journal` / `aura-erasure-journal-prod`, свой токен на бакет |
| Q2, Q3 — когда писать и как ограничить срок | «Джоба + правило 45 дней (Recommended)»: первый шаг джобы `account-erasure`, старые записи удаляет правило жизненного цикла бакета |
| Утверждение спеки | «Да, в работу» |

Спека `005` без изменений. Дальше код в `slave-2`, коммит — только после
ревью владельца. Бакеты и токены заводит владелец: шаг A до мёржа в `develop`,
шаг B до релиза в `main` (`005` §6).
