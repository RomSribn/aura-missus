# AURAT-0073-002 — Запуск в `aura-bff-manor` и проверка состояния

Дата: 2026-09-16
Где: `aura-bff-manor` / `slave-2`, ветка `feature/AURAT-0073-bff-erasure-journal`
(в обоих репозиториях), `develop` @ `781957a`, brain @ `0ed266f`.

## Что сказал владелец при запуске (близко к тексту)

- AURAT-0073 — журнал удалений аккаунтов вне базы и вне логов контейнера.
- Заглушка — `001-initial.md` (brain `0ed266f`). ID выдан `aura-app-manor`,
  новых не выпускать.
- Происхождение: находка 1 проверок `AURAT-0042-115` (D4 из `AURAT-0042-008`
  держится на логе `account erased`, который пропадает на деплое, и на таблице
  `account_erasures` в той же базе). На вопрос «Развилки из 115: правка
  продукта или текст в политике?» владелец ответил: «Да, всё как предложено
  (Recommended)» — журнал делаем правкой продукта в `aura-bff`.
- Хранилище журнала, бакет и токен — предложить в спеке. Новый бакет или токен
  в Cloudflare заводит владелец отдельным шагом до релиза в `main`.
- Мёрж в `develop` и релиз в `main` — только с одобрением владельца.

## Что нашлось

- Папка задачи есть, в ней только `001-initial.md` (статус «заведена, не
  начата»). Ветки под `AURAT-0073` не было — создана `wts-start`.
- Слот был свободен (detached HEAD), `active-work.md`: `slave-2` → busy.
- Связанное: `AURAT-0042` (удаление аккаунта; BFF-половина смержена, `015`
  approved), `AURAT-0042-115` (находка 1), `AURAT-0074` (логи без личных
  данных) — параллельно в `slave-3`, пересечение возможно только по логам
  `account erased`.

## Дальше

`003-understand.md`, затем сбор контекста по коду удаления, хранилищу R2 и
`AURAS-0004`.
