# AURAT-0073-018 — Шаг B и релиз в production

Дата: 2026-09-17
Где: манор `aura-app-manor`, сервер `37.27.199.90`. Секреты не выводились:
значения читались из Coolify во временные файлы `0600`, удалённые после
проверки.

## Шаг B

Бакет, правило и токен владелец создал в Cloudflare, переменные внёс в
Coolify сам.

| Проверка | Итог |
|---|---|
| Четыре `ERASURE_JOURNAL_*` в production `aura-bff` | есть; **пришли с флагом build-time** — снят в манор до деплоя, runtime-only |
| Бакет, endpoint | `aura-erasure-journal-prod`, EU; бакет и ключ не совпадают с dev |
| Токен в своём бакете | put, get, delete — ok; `erasures/` пуст |
| Прод-токен → `aura-erasure-journal`, dev-токен → прод-бакет | `AccessDenied` в обе стороны |
| Правило 45 дней | в первый раз **не было**: новые объекты шли без `Expiration`, хотя dev тем же кодом его показал. После правила от владельца — `rule-id="expire-45d"` и в `erasures/`, и в корне |

## Релиз

Владелец одобрил релиз `develop` → `main`: `781957a` → `abe2039`, fast-forward,
push в 08:41:04 UTC. Вместе с задачей ушла `AURAT-0074`.

- Деплои `aura-bff` и `aura-chatwoot` — `finished`.
- Прод-BFF:
  - `restarts=0`;
  - миграция `20260916200000_account_erasure_source` применена (21 из 21),
    столбец `account_erasures.source` на месте;
  - в контейнере все 4 переменные журнала;
  - `warn`/`error` с запуска — 0;
  - `/health` 200, вебхук без подписи и `DELETE /v1/me` без токена — 401.
- **~4 с `502` на старте.** Rolling update без healthcheck снимает старый
  контейнер сразу (`013`). Записано в `AURAS-0004` → *Deploys*.
- Запись в прод-журнал вживую не делалась: нет настоящего удаления. Путь
  проверен на dev (`016`), токен — выше.
